let { Get } = require('../../tools/request');
let ProgressBar = require('../../modules/ProgressBar');
let {_} = require('../../tools/commonModules');
let pb = new ProgressBar({ 'description': 'color', bar_length: 50 });


let Donwloader = (options,name,callback)=>{
    try{
        if (_.isUndefined(name)) {

        }
    }catch{}

}
